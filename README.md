The ChatApp/WebSite is still in production!
The App is in production, later a Website will also be create, which can also communicate with the App.

1. What has been done:
- Simple message exchanges using UDP
- Very basic UI(for now)

To do: Database, UI, Message encryption, image and video sharing, WebSite, Bug fixes
-------------------------------------
